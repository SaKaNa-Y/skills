interface:
  display_name: "Just Use It"
  short_description: "Experience public capabilities before reading source"
  default_prompt: "Use $just-use-it to experience this developer tool hands-on before inspecting its source and report every capability and finding."
policy:
  allow_implicit_invocation: false
